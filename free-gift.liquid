<!-- Sale Disabled Aug 2020 - Must include this snippet in cart.liquid <!-- {#% assign free_gift_over_price_100 = 10000 %}  -->
{% assign free_gift_over_price_150 = 15000 %}


<!-- Need Product Created  -->



{% assign variant_id = '32303080636503' %}
<!-- {#% assign variant_id_2 = '32197201264727' %} -->

  <script>
  (function($) {
    var cartItems = {{ cart.items | json }},
        qtyInTheCart = 0,
        cartUpdates = {},
        cartTotal = {{ cart.total_price }};
        
    for (var i=0; i<cartItems.length; i++) {
//       if ( cartItems[i].id === {#{# variant_id }} || cartItems[i].id === {#{# variant_id_2 }} ) {
      if ( cartItems[i].id === {{ variant_id }}  ) {
        qtyInTheCart = cartItems[i].quantity;
        break;
      }
    }
   
    if ( ( cartItems.length >= 1 ) && ( qtyInTheCart > 0 ) && (cartTotal < {{ free_gift_over_price_150 }}) ) {
      cartUpdates = { {{ variant_id }}: 0 }
    }    
  
  	else if ( ( cartItems.length >= 1 ) && ( qtyInTheCart !== 1 ) && (cartTotal >= {{ free_gift_over_price_150 }}) ) {
      cartUpdates = { {{ variant_id }}: 1 }
    }    
    
//     else if ( ( cartItems.length >= 1 ) && ( qtyInTheCart !== 1 ) && (cartTotal >= {{ free_gift_over_price_100 }} && cartTotal < {{ free_gift_over_price_150 }}) ) {
//       cartUpdates = { {{ variant_id }}: 1 }
//     }
             
//     else if ( ( cartItems.length >= 1 ) && ( qtyInTheCart !== 1 ) && (cartTotal >= {{ free_gift_over_price_150 }} ) ) {
//       cartUpdates = { {{ variant_id_2 }}: 1 }
//     }
  
//    else if ( ( cartItems.length >= 1 ) && ( qtyInTheCart !== 1 ) && (cartTotal < {{ free_gift_over_price_150 }}) ) {
//        cartUpdates = { {{ variant_id }}: 0 }
//   	return;
//    	}
           
    else {
      return;
    }

    
    var params = {
      type: 'POST',
      url: '/cart/update.js',
      data: { updates: cartUpdates },
      dataType: 'json',
      success: function() { 
        window.location.href = '/cart';
        
              }
    };
    $.ajax(params);
  })(jQuery);

  </script>
 -->
